name: Apply launch theme picker from PR

on:
  pull_request:
    branches: [main]

permissions:
  contents: write

jobs:
  apply-launch-picker:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0

      - name: Run registered launch-picker patch
        shell: bash
        run: |
          python3 - <<'PY'
          from pathlib import Path
          import re

          workflow = Path('.github/workflows/apply-launch-theme-picker.yml').read_text(encoding='utf-8')
          match = re.search(r"python3 - <<'PY'\n([\s\S]*?)\n          PY", workflow)
          if not match:
              raise RuntimeError('Could not locate launch-picker patch')
          script = '\n'.join(
              line[10:] if line.startswith('          ') else line
              for line in match.group(1).splitlines()
          )
          exec(compile(script, 'apply-launch-theme-picker.yml', 'exec'))
          PY

      - name: Commit launch picker to main
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add revive-member-sets.js revive-member-sets.css index.html members.html eras.html media.html
          git commit -m "Add launch theme picker and change hint"
          git push origin main
